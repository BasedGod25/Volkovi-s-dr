services:
  volkovi-game:
    build: .
    container_name: volkovi_game
    restart: always
    ports:
      - "8001:8000"
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    volumes:
      - ./data:/app/data
      - ./media:/app/media
